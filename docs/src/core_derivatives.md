# Derivatives

Tradeable instruments — `Buy`/`Sell` for the underlying, plus `LongCall`/`LongPut`/
`ShortCall`/`ShortPut` — dispatched through a common `payoff` interface.

```@autodocs
Modules = [Orcus]
Pages = ["Core/Derivative.jl"]
Private = false
```

```@index
Pages = ["core_derivatives.md"]
```
